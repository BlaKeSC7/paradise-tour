[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: todas las rutas (ej. /tour/2, /categorias) deben servir index.html
# para que React Router las maneje del lado del cliente.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
